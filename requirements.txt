# --- LangGraph / LangChain -------------------------------------------------
langgraph>=1.2,<2
langgraph-checkpoint-sqlite>=3.1
langchain-core>=1.0
langchain-anthropic>=1.0
langchain-openai>=1.0

# --- data modelling --------------------------------------------------------
pydantic>=2.9
python-dotenv>=1.0

# --- web research ----------------------------------------------------------
ddgs>=9.0                 # keyless DuckDuckGo search (default backend)
tavily-python>=0.8        # optional, better results, needs TAVILY_API_KEY

# --- rendering (Slide JSON -> HTML -> PNG) ---------------------------------
jinja2>=3.1
playwright>=1.48          # run `playwright install chromium` after installing
pillow>=10.0              # fallback renderer when no browser is available

# --- UI --------------------------------------------------------------------
streamlit>=1.38

# --- developer conveniences ------------------------------------------------
grandalf>=0.8             # ASCII graph diagrams for `--show-graph`

# --- optional: hosted tracing ----------------------------------------------
# LangSmith needs NO package - it activates from environment variables alone
# (langsmith ships as a langchain-core dependency).
#
# Langfuse needs both of these; install only if you set LANGFUSE_* keys:
#   pip install langfuse langchain
# The `langchain` umbrella package is required by Langfuse's LangChain
# integration - langchain-core on its own is not enough.
